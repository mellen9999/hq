name: ci
on: [push, pull_request]
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: syntax
        run: bash -n bin/hq bin/hq-poll bin/hq-status
      - name: shellcheck
        run: shellcheck bin/hq bin/hq-poll bin/hq-status robots/example.sh
